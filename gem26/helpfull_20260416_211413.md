# Gem Language — Complete Reference

**Generated:** 2026-04-16 21:14:13  
**Version:** 0.1.0 | **Built with:** C++26

---

## Keywords

| Keyword | Purpose |
|---------|----------|
| `fun name(p1,p2) ... end` | Define function; last expr is return value |
| `obj Name(params) : Parent ... end` | Define object/class (single inheritance) |
| `use "file"` | Include .g/.gm or AI-translate .py/.jl/.r/.cpp/.go/.rb/.rs/.js |
| `if cond ... else ... end` | Conditional (else optional) |
| `while cond ... end` | Loop while condition is truthy |
| `end [expr]` | Close block; optional return expr for fun |
| `alias name = expr` | REPL shortcut |
| `his` | Show today's session history |
| `lib` | List all loaded builtin modules |
| `exit` / `quit` | Exit interpreter |
| `langport(pattern,[out])` | AI-port foreign code to Gem |

## Types

| Type | Declaration | Notes |
|------|-------------|-------|
| `int` | `int x = 0` | Stores double; also holds vectors |
| `double` | `double pi = 3.14` | IEEE 754 double |
| `string` | `string s = "hi"` | UTF-8 string |
| `bool` | `bool ok = true` | true / false |
| `_var` | `_config = 1` | Underscore prefix = global scope |

Multi-var: `int a, b, c = 0`  Chained: `a = b = c = 0`

## Literals

`true` `false` `null` `nil` (=null) `nan` (IEEE 754 NaN)

## Operators

| Category | Operators |
|----------|-----------|
| Arithmetic | `+` `-` `*` `/` |
| Compound assign | `+=` `-=` `*=` `/=` |
| Comparison | `==` `!=` `>` `>=` `<` `<=` |
| Logical | `!` (prefix not) `&&` `||` |
| Separator | `;` (multiple statements per line) |
| String concat | `+` |

## Builtin Functions

| Function | Params | Returns | Notes |
|----------|--------|---------|-------|
| `isnil(x)` | any | bool | true if x is nil/null/void |
| `isnan(x)` | any | bool | true if x is IEEE 754 NaN |
| `tonum(x)` | any | double | parse to number; nan on failure |
| `tostr(x)` | any | string | convert to string |

## Vectors

```
int v = [1.0, 2.0, 3.0]   # creation
v[0]                       # index (0-based)
v.length / v.size / v.count  # length
```

## Builtin Modules (35)

### sys — System Root (inherited by all objects)
| Method | Signature | Returns |
|--------|-----------|--------|
| `print` | `print(...)` | void |
| `args` | `args()` | vector\<string\> |
| `async` | `async(func,[params],[timeout])` | thread handle |
| `sethandler` | `sethandler(signals,func)` | bool |
| `host` | `host()` | string |
| `exec` | `exec(cmd)` | int (exit code) |
| `doc` | `doc([path])` | string |
| `help` | `help([topic])` | void |
| `exit` | `exit()` | void |
| `langport` | `langport(pattern,[output])` | bool |
| `redirect` | `redirect(url,[port])` | bool |
| `app` | `app(port,[routes])` | bool |

### math — Mathematics & Symbolic Math
**Numeric:** `sin(x)` `cos(x)` `sqrt(x)` `math.pi`  
**Symbolic (SymPy/Sage):** `useSymPy()` `useSage()` `diff(expr,[var])` `integrate(expr,[var])` `simplify(expr)` `solve(expr,[var])` `sym_latex(expr)` `to_latex(val)` `write_latex(path,content,[standalone])` `read_latex(path)` `parse_latex(text)` `compile_latex(path)`

### ai — AI Integration (Gemini/Mistral/Ollama)
| Method | Signature | Returns |
|--------|-----------|--------|
| `prompt` | `prompt(text)` | string |
| `prompt_native` | `prompt_native(text)` | string |
| `useGemini` | `useGemini()` | bool |
| `useMistral` | `useMistral(model)` | bool |
| `useOllama` | `useOllama(model,[host])` | bool |
| `setKey` | `setKey(key)` | bool |
| `setHost` | `setHost(host)` | bool |
| `setPath` | `setPath(path)` | bool |
**Properties:** `ai.provider` `ai.model` `ai.host`

### text — String & Document Processing
`read(path)→string` `sub(s,start,[len])→string` `replace(s,old,new)→string`  
`write_pdf(path,text)→bool` `write_pdf_a(path,text)→bool` `read_pdf(path)→string`  
`read_markdown(path)` `write_markdown(path,text)` `read_yaml(path)` `write_yaml(path,obj)`  
`read_html(path)` `write_html(path,text)` `read_xml(path)` `write_xml(path,text)`  
`read_fits_header(path)→object` `read_hdf_header(path)→object`

### rex — Regular Expressions (ECMAScript)
All accept optional `flags` string (`"i"` = case-insensitive):  
`match(text,pattern,[flags])→bool` `find(text,pattern,[flags])→string`  
`findall(text,pattern,[flags])→list` `groups(text,pattern,[flags])→list`  
`sub(text,pattern,repl,[flags])→string` `gsub(text,pattern,repl,[flags])→string`  
`split(text,pattern,[flags])→list` `count(text,pattern,[flags])→int`

### algo — Algorithms, Sorting & Time
`add(...)→double` `quicksort(v)→vector` `sort(v,[start],[end])→vector`  
`now()→string` `date_add(ts,days)→double` `date_diff(t1,t2)→double`

### geo — GIS & Geolocation
`lookup()→{.lat,.lon,.city,.country}` `distance(lat1,lon1,lat2,lon2)→double`  
`write_geojson(path,features)→bool` `history(plate)→string`  
`plot2d(data,[layout])→string` `plot3d(data,[layout])→string`

### astro — Astrophysics & Planetary Science
**Constants:** `G` `c` `AU` `pc` `ly` `Msun` `Rsun` `Lsun` `Tsun` `Mearth` `Rearth` `H0` `sigma_sb`  
**Unit conv:** `to_ly(pc)` `to_pc(ly)` `to_au(m)` `deg_to_rad(d)` `rad_to_deg(r)`  
**Stellar:** `luminosity(R,T)` `stefan_boltzmann(T,R)` `wien(T)` `abs_magnitude(m,d_pc)` `distance_modulus(d_pc)` `spectral_class(T)` `schwarzschild_radius(M)` `escape_velocity(M,R)`  
**Orbital:** `orbital_period(a_au,M_msun)` `orbital_velocity(a_m,M_kg)` `hill_sphere(a,mp,ms)` `roche_limit(R,rho_p,rho_s)` `synodic_period(p1,p2)` `planet(name)`  
**Solar:** `solar_flux(dist_au)` `solar_wind_pressure(n,v)` `sunspot_cycle(year)` `parker_spiral_angle(v,d)` `solar_activity()`  
**Cosmology:** `hubble_distance(z)` `redshift_velocity(z)` `lookback_time(z)` `critical_density()`  
**Coords:** `equatorial_to_galactic(ra,dec)` `angular_separation(ra1,dec1,ra2,dec2)`  
**Exoplanet:** `transit_depth(rp,rs)` `habitable_zone(L)` `equilibrium_temp(L,d,A)`  
**Pulsar:** `pulsar_spindown(P,Pdot)→{age_yr,Bfield_G,edot_W}`

### fin — Financial Engineering (QuantLib + yfinance)
`ticker(symbol)→{.price,.volume,.open,.high,.low,.close,.pe_ratio,.market_cap,.dividend_yield}`  
`high_yield_bonds()` `high_yield_etfs()` `high_yield_equities()` `dashboard()`  
`bs_price(type,strike,spot,rate,vol,t)→double`  
`greeks(type,strike,spot,rate,vol,t)→{.npv,.delta,.gamma,.theta,.vega}`  
`date(d,m,y)→{.day,.month,.year,.serial,.str}` `calendar(name)→object`  
`is_holiday(cal,d,m,y)→bool` `add_days(d,m,y,n)→{.day,.month,.year,.str}` `diff_days(d1,m1,y1,d2,m2,y2)→double`

### bsm — Black-Scholes-Merton (extends fin)
`price_american(symbol,type,duration)→double`  duration: `"weekly"` `"monthly"` `"quarterly"`

### chart — Interactive Plotting (Plotly.js)
`plot(traces,[layout])→string` `show(path)→bool` `server(path,[port])→bool`

### data — Data Science
`read_csv(path)→vector` `mean(vector)→double` `std(vector)→double`

### tcp — TCP/IP Networking
`listen(port)→int` `accept(fd)→{.fd,.addr}` `connect(host,port)→int`  
`send(fd,msg)→int` `recv(fd,[size])→string` `close(fd)→bool`  
`nic()→[{.name,.ip,.status}]` `routes()→[{.dest,.gw,.iface}]`

### thread — Background Thread Handle (from sys.async)
`wait()→any` `is_finished()→bool`

### www — Web Framework
`wget(url,file)→bool` `app(port,[routes])→bool` `redirect(target,[port])→bool` `map2d(geojson,output)→bool`

### cdn — Caching Proxy (extends www)
`start(port,routes_map)→bool` `stats()→{.hits,.misses,.bytes,.cached_items}` `purge([path])→bool` `config(type,content)→string` `dashboard()→bool`

### itr — Iterators
`range(n)→[0..n-1]` `while(cond_fun,body_fun)→void`

### bev — Bevington Data Reduction
`data(x_vec,y_vec)→bool` `fit_line()→bool` `param(idx)→double`  idx: 0=intercept, 1=slope

### file — Filesystem
`write(path,content)→bool` `exists(path)→bool`

### zip — Compression
`compress(src,archive)→bool` `decompress(archive,dest)→bool`

### img — Image Processing (ImageMagick)
`resize(src,width,height,dest)→bool`

### nlp — Natural Language Processing
Delegates to `ai.prompt()`. Used internally by `mobl.dictate()`.

### cpp — C++26 JIT (Cling)
`repl()→int` `exec(code)→int`

### k3s — Docker & Kubernetes
`docker_run(image,cmd)` `docker_ps()` `docker_build(path,tag)` `docker_stop(id)`  
`k3s_apply(yaml)` `k3s_get(resource)` `k3s_pods()` `k3s_nodes()` `k3s_logs(pod)`

### vm — Vagrant VMs
`init(box)` `up()` `ssh(cmd)` `status()` `halt()` `destroy()`

### mobl — Mobile PWA
`phone(name)→object` `dictate(text)→{title,note,tags}` `make_feature(lat,lon,text)→GeoJSON`

### trek — Travel Logs & OSM
`new(path)` `add(path,lat,lon,[title],[note])` `edit(path,idx,title,note)` `remove(path,idx)`  
`load(path)→string` `show(path,[port])` `export_gpx(geojson,gpx)` `stats(path)→{.waypoints,.distance_km}`

### seo — Search Engine Optimization
`index(urls,output_path)→bool` `analyze(index_path)→void`

### drvr — Device Drivers
`linux(name)` `win11(name)` `macos(name)` `android(name)` `build(target)` `deploy(target)`

### art — ASCII Art & SVG
`text_to_art(text,[font])→string` `art_to_file(text,path)→bool` `art_to_svg(text,[path])→string`  
`svg_to_art(svg_path,[width])→string` `mindmap([path])→string` `readme([path])→string` `tutorial([path])→string`

### python — Python 3 Polyglot
`run(file_or_code)→int` `compile(file)→int` `pip(pkg)→int`  
Runs `.py` files or inline Python natively. `compile()` byte-compiles via `py_compile`. `pip()` installs packages.

### cython — Cython Polyglot
`run(pyx)→int` `compile(pyx,[out])→int` `build(pyx)→int` `pip(pkg)→int`  
Pipeline: `.pyx` → `cython -3` → `.c` → `gcc -shared -fPIC` → `.so`. `build()` produces importable shared library. `run()` compiles and executes. Prerequisites: `cython`, `gcc`, `python3-dev`.

### go / ruby / node / rust — Polyglot
`run(input)→string`  
`go.build(file)` `node.npm_install(pkg)` `rust.cargo_new(name)`

## CLI

```
gem <file.g>              Run script
gem -c <main.g> [mods...] Compile to binary
gem -o <name>             Output name
gem -h                    Print history and exit
gem -t <file> [-o out]    AI-translate to Gem
gem                       Start REPL
```

## REPL

- Expressions print `=> value`
- Arrow keys: readline history
- History saved to `~/.gem/YYYYDDD.txt`
- `his` — show today's log
- `lib` — list modules
- `help "topic"` — detailed help
- `alias ? = sys.help()` — define shortcut

## Object System

```
obj Name(params) : Parent
  .attr = val        # own attribute
  ..attr = val       # parent attribute
  fun method(p) ... end
end
Name var = Name(args)
```
All objects inherit from `sys`. Single inheritance only.

---
*Generated by `sys.help("all")`*
